apiVersion: v1
kind: Secret
metadata:
  name: fusion-gateway-secrets
  namespace: fusion-gateway
type: Opaque
stringData:
  master_key: "CHANGE_ME_MASTER_KEY"
  # Add API keys as needed:
  # api_key_demo: "fg-demo-key-change-me"
