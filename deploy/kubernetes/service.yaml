apiVersion: v1
kind: Service
metadata:
  name: fusion-gateway
  namespace: fusion-gateway
  labels:
    app: fusion-gateway
spec:
  type: ClusterIP
  ports:
    - port: 8100
      targetPort: 8100
      protocol: TCP
  selector:
    app: fusion-gateway
