apiVersion: v1
kind: ConfigMap
metadata:
  name: fusion-gateway-config
  namespace: fusion-gateway
data:
  config.yaml: |
    server:
      host: "0.0.0.0"
      port: 11432
    auth:
      master_key: "${FG_MASTER_KEY}"
    route:
      enable_hardware_judge: false
      token_threshold: 50
