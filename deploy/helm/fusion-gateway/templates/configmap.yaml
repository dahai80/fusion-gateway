apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fusion-gateway.fullname" . }}-config
  labels:
    {{- include "fusion-gateway.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
